// import { IPFS } from '../app/services/ipfs.js'
// // const ipfs = new IPFS();

// describe('getDatabse()', async () => {
//     beforeEach(() => {
//         await ipfs.clearDatabase();
//     });

//     it('should return populated data', () => {
//         expect.assertions(1);
        
//         const returnedData = ipfs.getDatabse();
//         expect(Object.keys(returnedData).length).toBe(0);
//     });

//     it('should return empty data', () => {
//         expect.assertions(1);
//         const returnedData = await ipfs.getDatabse();
//         expect(Object.keys(returnedData).length).toBe(0);
//     });
// });